#include <bits/stdc++.h>
#define int long long
using namespace std;

signed main () {
    int n, m; cin >> n >> m;
    int x[n]; string t[n];
    for (int i = 0; i < n; i++) {
        cin >> x[i] >> t[i];
    }

    return 0;
}